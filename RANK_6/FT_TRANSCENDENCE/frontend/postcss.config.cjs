// tells PostCSS which plugins to use
module.exports = {
  plugins: {
    tailwindcss: {},   // Runs Tailwind to generate utility classes
    autoprefixer: {},  // Adds vendor prefixes for better browser support
  },
};